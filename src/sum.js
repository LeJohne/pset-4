const readlineSync = require("readline-Sync");
const lowBound = Number(readlineSync.question("Lower bound: "));
const upperBound = Number(readlineSync.question("Upper bound: "));

if (lowBound < Number.MIN_SAFE_INTEGER || upperBound > Number.MAX_SAFE_INTERGER || lowerBound < upperBound) {
  const readlineSync = require("readline-Sync");
  const lowBound = Number(readlineSync.question("Lower bound: "));
  const upperBound = Number(readlineSync.question("Upper bound: "));
}else if (upperBound % 2 == 0){

}else if (upperBound - 1)% 2 == 0)
  while ()
